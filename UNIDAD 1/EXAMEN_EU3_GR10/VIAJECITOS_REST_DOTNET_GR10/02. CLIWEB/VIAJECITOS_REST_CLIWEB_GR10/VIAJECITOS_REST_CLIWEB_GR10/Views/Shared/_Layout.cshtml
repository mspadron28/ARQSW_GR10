<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <title>Viajecitos SA - @ViewData["Title"]</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        /* Paleta de colores pastel para Viajecitos SA */
        :root {
            --primary-blue: #A3BFFA; /* Azul pastel claro */
            --secondary-blue: #BFDBFE; /* Celeste suave */
            --accent-blue: #60A5FA; /* Azul más vibrante para botones */
            --background: #F1F5F9; /* Fondo gris claro con toque pastel */
            --text-dark: #1E3A8A; /* Azul oscuro para texto */
            --black: #000000;
            --white: #ffffff;
            --error-red: #EF4444; /* Rojo para errores */
        }

        /* Estilos generales */
        body {
            font-family: 'Roboto', Arial, sans-serif;
            margin: 0;
            background-color: var(--background);
            color: var(--text-dark);
            line-height: 1.6;
        }

        /* Encabezado */
        .header {
            background: linear-gradient(to bottom, var(--primary-blue), var(--secondary-blue));
            padding: 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

            .header img {
                width: 300px;
                height: 100px;
                object-fit: cover;
                border-radius: 8px;
            }

        .header-text {
            text-align: right;
            color: var(--white);
        }

            .header-text h1 {
                margin: 0;
                font-size: 2.5rem;
                font-weight: 700;
            }

            .header-text p {
                margin: 5px 0 0;
                font-size: 1.1rem;
            }

        /* Contenedor principal para el contenido */
        .main-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        /* Encabezado del formulario */
        .form-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

            .form-header h2 {
                margin: 0;
                font-size: 1.8rem;
                color: var(--text-dark);
            }

            .form-header .view-purchases-btn {
                padding: 10px 20px;
                background-color: var(--accent-blue);
                color: var(--white);
                border: none;
                border-radius: 6px;
                font-size: 1rem;
                cursor: pointer;
                transition: background-color 0.3s ease;
                text-decoration: none;
            }

                .form-header .view-purchases-btn:hover {
                    background-color: #2563EB;
                }

        /* Formulario */
        .form-container {
            background-color: var(--white);
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
            max-width: 600px; /* Aumentado para formularios más amplios */
            width: 100%;
            margin: 0 auto;
        }

        .form-group {
            margin-bottom: 24px; /* Más espaciado entre campos */
        }

            .form-group label {
                display: block;
                font-weight: 500;
                margin-bottom: 10px;
                color: var(--text-dark);
                font-size: 1.1rem;
            }

            .form-group input,
            .form-group select {
                width: 100%;
                padding: 14px; /* Más padding para inputs más grandes */
                border: 1px solid var(--primary-blue);
                border-radius: 6px;
                font-size: 1.1rem;
                background-color: var(--secondary-blue);
                transition: border-color 0.3s ease;
                box-sizing: border-box;
            }

                .form-group select:focus,
                .form-group input:focus {
                    outline: none;
                    border-color: var(--accent-blue);
                    background-color: var(--white);
                }

        button {
            width: 100%;
            padding: 14px;
            background-color: var(--accent-blue);
            color: var(--white);
            border: none;
            border-radius: 6px;
            font-size: 1.2rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

            button:hover {
                background-color: #2563EB;
            }

        /* Disposición inline para origen y destino */
        .form-group-inline {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .select-group {
            flex: 1;
        }

        .dash {
            font-size: 1.2rem;
            color: var(--text-dark);
            font-weight: 500;
        }

        /* Tabla para compras */
        .table-container {
            background-color: var(--white);
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
            max-width: 700px;
            margin: 0 auto;
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

            .table th, .table td {
                border: 1px solid var(--primary-blue);
                padding: 12px;
                text-align: left;
            }

            .table th {
                background-color: var(--accent-blue);
                color: var(--white);
                font-weight: 500;
            }

            .table tr:nth-child(even) {
                background-color: var(--secondary-blue);
            }

            .table tr:hover {
                background-color: var(--primary-blue);
            }

        /* Contenedor de resultados */
        .result-container {
            background-color: var(--white);
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            margin: 0 auto;
        }

        .flight-info p {
            margin: 10px 0;
            font-size: 1rem;
        }

        /* Errores */
        .error {
            color: var(--error-red);
            margin: 10px 0;
            font-weight: 500;
            text-align: center;
        }

        /* Enlaces de navegación */
        .nav-links {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }

            .nav-links a {
                padding: 10px 20px;
                background-color: var(--secondary-blue);
                color: var(--text-dark);
                text-decoration: none;
                border-radius: 6px;
                font-weight: 500;
                font-size: 1rem;
                transition: background-color 0.3s ease;
            }

                .nav-links a:hover {
                    background-color: var(--primary-blue);
                }
    </style>
</head>
<body>
    <header class="header">
        <img src="~/images/travel-agency.jpg" alt="Viajecitos SA">
        <div class="header-text">
            <h1>Viajecitos SA</h1>
            <p>Encuentra y compra tus boletos de avión de forma fácil y segura</p>
        </div>
    </header>
    <div class="main-content">
        @RenderBody()
    </div>
</body>
</html>